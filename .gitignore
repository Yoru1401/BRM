/target

# Working memory for the AI sessions - notes, decisions, benchmarks.
# Local only, on purpose.
/memory

# Local-only: a SessionStart hook that reads memory/, which is itself local.
/.claude/settings.local.json
